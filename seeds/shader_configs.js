
exports.seed = function(knex) {
  // Deletes ALL existing entries
  /*
  return knex('shader_configs').del()
    .then(function () {
      // Inserts seed entries
      return knex('shader_configs').insert([

      ]);
    });
    */
   return
};
